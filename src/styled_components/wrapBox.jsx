import styled from "styled-components";

const WrapBox = styled.div`
  width: 100%;
  z-index: 1;
  padding-top: 115px;
  overflow: hidden;
  min-height: 100vh;
  position: relative;
  padding-bottom: 60px;
`;

export default WrapBox;
